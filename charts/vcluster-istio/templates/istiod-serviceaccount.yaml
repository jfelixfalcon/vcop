{{- if .Values.istiod.enabled }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: istiod
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "vcluster-istio.labels" . | nindent 4 }}
    app: istiod
{{- end }}
